// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("human", "for human support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("human", "enable human support", "no");

if (PHP_HUMAN != "no") {
	EXTENSION("human", "human.c");
}

